module.exports = {
  name: 'JS Playground',
  desc: 'Not sure',
  suite: 'Javascript',
  tags: ['JAVASCRIPT'],

  test: (page) => async () => {
    // let tags = await page.getScripts()
    // debugger
    //
    // tags.forEach(tag => {
    //   console.log(typeof(tag))
    // })

    expect(content).not.toBeNull()
  }
}
