module.exports = class TesterBot {
  run( ){
let urls = []

    // For each URL
    for (let url in urls){
      // Run Suite (filtered by tags)

      describe(test.name, () =>{

        //for each test case
          test.testCase()
      })
    }

  }
}